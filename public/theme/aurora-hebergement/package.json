{
  "name": "current",
  "version": "1.0.0",
  "scripts": {
    "watch": "chokidar '*.config.js' '**/*.twig' -i 'node_modules' -c 'yarn build-tailwind' --initial"
  },
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "chokidar-cli": "^2.1.0",
    "cssnano": "^5.0.6",
    "postcss-cli": "^8.3.1"
  }
}
